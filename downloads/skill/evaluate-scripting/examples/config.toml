# Defaults for the player behavior (player.behavior.evt).
# Exposed to Lua as the `config.player` table. Note what is NOT here anymore:
# the start position is frontmatter `properties:` (static engine state), and
# move speed is the `speed` attribute (sprint.behavior.evt) so effects can
# modify it — config is for plain tunables shared by every instance.
[player]
base_weapon_damage = 25.0
