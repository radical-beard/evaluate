# A swappable scene. Its nodes live under a per-scene container that is freed
# wholesale when we leave the scene (goto). Parent-by-name builds the tree.
[[node]]
name = "MenuRoot"
type = "Node3D"

[[node]]
name = "Title"
type = "Node3D"
parent = "MenuRoot"
position = [0.0, 1.0, 0.0]
